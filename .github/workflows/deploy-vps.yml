name: Deploy to VPS

# This runs alongside GitHub Pages, not instead of it.
# GitHub Pages continues to auto-deploy from the `main` branch as before —
# this workflow just also mirrors the site to a self-hosted VPS.
#
# Auto-trigger on push is disabled for now (VPS secrets aren't set up yet,
# so every push was failing and emailing an error). Once the VPS_* secrets
# are added under Settings > Secrets and variables > Actions, re-enable the
# `push` trigger below, or just run this manually via the Actions tab.

on:
  workflow_dispatch:
  # push:
  #   branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to VPS via rsync over SSH
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avzr --delete --exclude=".git*" --exclude=".github"
          path: ./
          remote_path: ${{ secrets.VPS_DEPLOY_PATH }}
          remote_host: ${{ secrets.VPS_HOST }}
          remote_user: ${{ secrets.VPS_USER }}
          remote_key: ${{ secrets.VPS_SSH_KEY }}
          remote_port: ${{ secrets.VPS_PORT }}